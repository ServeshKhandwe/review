spring.application.name=coupon-service

# Disable SQL initialisation by default — only dev enables it.
# Prevents data.sql test seed from running in prd or inmem profiles.
spring.sql.init.mode=never
spring.jpa.open-in-view=false
